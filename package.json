{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "babel --stage 0 lib/plugin.js > dist/plugin.dist.js",
    "demo": "babel --stage=0 --plugins ./dist/plugin.dist.js --out-dir demo/ source/",
    "watch": "nodemon -e js,jsx --watch lib/plugin.js --watch demo/main.js --watch source --exec 'npm run start && npm run demo && npm run browserify'",
    "browserify": "browserify demo/main.js > demo/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel": "^5.8.23",
    "handlebars": "^4.0.2"
  },
  "devDependencies": {
    "browserify": "^11.1.0",
    "classnames": "^2.1.3",
    "nodemon": "^1.5.0"
  }
}
